<UserControl x:Class="ESystems.WebCamControl.View.Controls.WebcamVideoControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ESystems.WebCamControl.View.Controls"
             xmlns:viewModel="clr-namespace:ESystems.WebCamControl.ViewModel;assembly=ESystems.WebCamControl.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance viewModel:WorkspaceViewModel}">
    <StackPanel Orientation="Horizontal">
        <Button Width="50" Margin="2" Command="{Binding StartCaptureCommand}" CommandParameter="{Binding CameraSelectedItem}">Start</Button>
        <Button Width="50" Margin="2" Command="{Binding StopCaptureCommand}" CommandParameter="{Binding CameraSelectedItem}">Stop</Button>
    </StackPanel>
</UserControl>
